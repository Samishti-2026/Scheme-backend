# ─── LOCAL DEVELOPMENT ────────────────────────────────────────────────────────
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=root
# DB_DATABASE=Common-DB

# ─── RENDER (PRODUCTION) ──────────────────────────────────────────────────────
# Common DB (Oregon) — main app DB (tenants, users, etc.)
# DB_HOST=dpg-d75p3umdqaus73bg9iq0-a.oregon-postgres.render.com
# DB_PORT=5432
# DB_USERNAME=commondb_2ghk_user
# DB_PASSWORD=<your_password>
# DB_DATABASE=commondb_2ghk

# Tenant DB (Singapore) — store this as connectionUri in the tenant record
# TENANT_DB_URL=postgresql://scheme_tool_db_user:<password>@dpg-d75mv4juibrs73bqltrg-a.singapore-postgres.render.com/scheme_tool_db

# ─── ACTIVE CONFIG (fill in for your environment) ─────────────────────────────
DB_HOST=
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=
PORT=3000

# AES-256 key for encrypting tenant DB connection strings (32 bytes hex)
# NEVER commit a real key — rotate this in production
DB_ENCRYPTION_KEY=

# JWT — change this to a long random secret in production
JWT_SECRET=
JWT_EXPIRES_IN=8h
